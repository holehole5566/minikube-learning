apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "values-demo.fullname" . }}-test-connection"
  labels:
    {{- include "values-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "values-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
