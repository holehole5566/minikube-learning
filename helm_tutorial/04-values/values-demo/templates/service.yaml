apiVersion: v1
kind: Service
metadata:
  name: {{ include "values-demo.fullname" . }}
  labels:
    {{- include "values-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "values-demo.selectorLabels" . | nindent 4 }}
