apiVersion: v1
kind: Service
metadata:
  name: {{ include "templating-demo.fullname" . }}
  labels:
    {{- include "templating-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "templating-demo.selectorLabels" . | nindent 4 }}
