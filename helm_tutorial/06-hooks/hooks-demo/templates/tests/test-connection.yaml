apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hooks-demo.fullname" . }}-test-connection"
  labels:
    {{- include "hooks-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hooks-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
